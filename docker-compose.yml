version: '3'
services:
  # Application Dockerfile is in same folder which is .
  app:
    build: .

  # database is in database folder
  database:
    build: database/.
    ports:
      - "4000:4000"
    restart: always